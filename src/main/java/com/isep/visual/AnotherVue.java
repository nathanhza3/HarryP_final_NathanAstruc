package com.isep.visual;

import javafx.scene.Node;
import javafx.scene.Parent;

public class AnotherVue extends Parent {
    @Override
    public Node getStyleableNode() {
        return super.getStyleableNode();
    }
}
